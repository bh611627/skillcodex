{
  "name": "skillcodex",
  "private": true,
  "description": "SkillCodex - AI agent skills system and reusable workflows for React / Next.js (monorepo)",
  "scripts": {
    "validate": "cd package && pnpm run validate",
    "build": "cd package && pnpm run build",
    "sync": "cd package && pnpm run sync",
    "new-skill": "tsx scripts/new-skill.ts",
    "export-skills-sh": "tsx scripts/export-skills-sh-catalog.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@9.15.0"
}
